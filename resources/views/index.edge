<!DOCTYPE html>
<html lang="fr">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">

    @viteReactRefresh()
    @vite(['frontend/src/app.tsx'])

		<title>Galaguerre</title>

        <script type="module">
          import { Octokit } from "https://esm.sh/@octokit/core";

        // 1. Initialise avec ton Personal Access Token
        window.octokit = new Octokit();

        // // 2. Récupère tous tes anciens projets (Remplace owner et repo)
        // var projects = await octokit.request('GET /repos/{owner}/{repo}/projects', {
        // owner: 'TON_NOM_UTILISATEUR_OU_ORGANISATION', 
        // repo: 'NOM_DU_DEPOT',            
        // headers: { 'X-GitHub-Api-Version': '2022-11-28' }
        // });
        // console.log("Projets :", projects);

        // // Regarde dans la console l'ID du projet qui t'intéresse pour l'étape suivante.

        // // 3. (Optionnel) Récupère les informations du projet avec son ID
        // var project_details = await octokit.request('GET /projects/{project_id}', {
        // project_id: 'ID_DU_PROJET', // Remplace par l'ID trouvé à l'étape 2
        // headers: { 'X-GitHub-Api-Version': '2022-11-28' }
        // });
        // console.log("Détails du projet :", project_details);

        // // 4. Récupère les colonnes du projet (ex: "To Do", "In Progress", "Done")
        // var columns = await octokit.request('GET /projects/{project_id}/columns', {
        // project_id: 'ID_DU_PROJET', // Remplace par l'ID du projet
        // headers: { 'X-GitHub-Api-Version': '2022-11-28' }
        // });
        // console.log("Colonnes :", columns);

        // // Regarde dans la console les IDs de tes colonnes pour l'étape suivante.

        // // 5. Récupère les cartes contenues dans une colonne spécifique
        // var cards = await octokit.request('GET /projects/columns/{column}/cards', {
        // column: 'ID_DE_LA_COLONNE', // Remplace par l'ID d'une colonne trouvée à l'étape 4
        // headers: { 'X-GitHub-Api-Version': '2022-11-28' }
        // });
        // console.log("Cartes de la colonne :", cards);
        </script>
</head>
<body>
    <div id="app"></div>
</body>
</html>